<nav class="navbar navbar-default navbar-static-top menubar" role="navigation">
  <div class="container menubar-container">
<!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-inner">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".interest-bar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse interest-bar">
        <ul class="nav navbar-nav">
          <!-- <li><%#= link_to "About", interest_about_path(@interest) %></li> -->
          <li><%= link_to "Projects", interest_path(@interest) %></li>
          <li><%= link_to "#{@followers.count} Followers", interest_followers_path(@interest) %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <div class="btn-follow-unfollow">
            <% if user_signed_in? %>
              <% if current_user.interests.include?(@interest) %>
                <button type="button" class="btn btn-info btn-sm navbar-btn">
                  <%= link_to UserInterest.find_by_user_id_and_interest_id(current_user.id, @interest.id), method: :delete, :class=>"no-underline" do %>
                    <!--<span class="glyphicon glyphicon-ok"></span>-->  Following
                  <% end %>
                </button>
              <% else %>
                <button type="button" class="btn btn-info btn-sm navbar-btn">
                  <%= link_to user_interests_path(id: @interest), method: :post, :class=>"no-underline" do %>
                    <!-- <span class="glyphicon glyphicon-plus"></span> -->  Follow
                  <% end %>
                </button>
              <% end %>
            <% end %> 
          </div>
          <!-- FIXME 1 Follower"s" -->
         </ul>
      </div><!-- /.navbar-collapse -->
    </div>
  </div>
</nav>